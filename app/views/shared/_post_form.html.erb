<%= form_with model: post, local: true, html: { multipart: true, class: "needs-validation" } do |form| %>
<div class="mb-3">
    <%= form.label :title, "Titre", class: "form-label" %>
    <%= form.text_field :title, class: "form-control", required: true %>
</div>

<div class="mb-3">
    <%= form.label :content, "Description", class: "form-label" %>
    <%= form.text_area :content, class: "form-control", rows: 4, required: true %>
</div>

<div class="mb-3">
    <%= form.label :media, "Images / Vidéos", class: "form-label" %>
    <%= form.file_field :media, multiple: true, class: "form-control" %>
</div>

<%= form.submit "🚀 Publier", class: "btn btn-primary w-100" %>
<% end %>